import React from 'react';

const Home = () => {
  return (
    <div>
      <div>Im home component</div>
      <button onClick={() => console.log('hey')}>Click me</button>
    </div>
  );
};

export default Home;
